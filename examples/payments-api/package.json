{
  "name": "payments-api",
  "version": "1.0.0",
  "description": "Minimal reference service used to demonstrate end-to-end CrowsNest release-gate integration on real artifacts.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test server.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pino": "^9.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
